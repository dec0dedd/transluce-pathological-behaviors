hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
  launcher:
    _target_: hydra._internal.core_plugins.basic_launcher.BasicLauncher
  sweeper:
    _target_: hydra._internal.core_plugins.basic_sweeper.BasicSweeper
    max_batch_size: null
    params: null
  help:
    app_name: ${hydra.job.name}
    header: '${hydra.help.app_name} is powered by Hydra.

      '
    footer: 'Powered by Hydra (https://hydra.cc)

      Use --hydra-help to view Hydra specific help

      '
    template: '${hydra.help.header}

      == Configuration groups ==

      Compose your configuration from those groups (group=option)


      $APP_CONFIG_GROUPS


      == Config ==

      Override anything in the config (foo.bar=value)


      $CONFIG


      ${hydra.help.footer}

      '
  hydra_help:
    template: 'Hydra (${hydra.runtime.version})

      See https://hydra.cc for more info.


      == Flags ==

      $FLAGS_HELP


      == Configuration groups ==

      Compose your configuration from those groups (For example, append hydra/job_logging=disabled
      to command line)


      $HYDRA_CONFIG_GROUPS


      Use ''--cfg hydra'' to Show the Hydra config.

      '
    hydra_help: ???
  hydra_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][HYDRA] %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
    root:
      level: INFO
      handlers:
      - console
    loggers:
      logging_example:
        level: DEBUG
    disable_existing_loggers: false
  job_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][%(name)s][%(levelname)s] - %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
      file:
        class: logging.FileHandler
        formatter: simple
        filename: ${hydra.runtime.output_dir}/${hydra.job.name}.log
    root:
      level: INFO
      handlers:
      - console
      - file
    disable_existing_loggers: false
  env: {}
  mode: RUN
  searchpath: []
  callbacks: {}
  output_subdir: .hydra
  overrides:
    hydra:
    - hydra.mode=RUN
    task:
    - ~actor_rollout_ref.rollout.engine_kwargs.vllm.disable_mm_preprocessor_cache
    - +ray_init.runtime_env.pip=null
    - +ray_init.runtime_env.working_dir=/root/transluce-pathological-behaviors
    - algorithm.adv_estimator=grpo
    - data.train_files=prepared/train.parquet
    - data.val_files=prepared/train.parquet
    - data.train_batch_size=1
    - trainer.balance_batch=True
    - data.max_prompt_length=2048
    - data.max_response_length=1024
    - data.filter_overlong_prompts=True
    - data.truncation=error
    - actor_rollout_ref.model.path=Qwen/Qwen2.5-0.5B-Instruct
    - actor_rollout_ref.actor.optim.lr=1e-6
    - actor_rollout_ref.model.use_remove_padding=True
    - actor_rollout_ref.actor.ppo_mini_batch_size=1
    - actor_rollout_ref.actor.ppo_micro_batch_size_per_gpu=1
    - actor_rollout_ref.actor.use_kl_loss=True
    - actor_rollout_ref.actor.kl_loss_coef=0.001
    - actor_rollout_ref.actor.kl_loss_type=low_var_kl
    - actor_rollout_ref.actor.strategy=fsdp
    - actor_rollout_ref.model.enable_gradient_checkpointing=False
    - actor_rollout_ref.actor.fsdp_config.param_offload=False
    - actor_rollout_ref.actor.fsdp_config.optimizer_offload=False
    - actor_rollout_ref.rollout.log_prob_micro_batch_size_per_gpu=1
    - actor_rollout_ref.rollout.tensor_model_parallel_size=1
    - actor_rollout_ref.rollout.name=vllm
    - actor_rollout_ref.rollout.gpu_memory_utilization=0.4
    - actor_rollout_ref.rollout.n=4
    - actor_rollout_ref.ref.log_prob_micro_batch_size_per_gpu=1
    - algorithm.use_kl_in_reward=False
    - trainer.critic_warmup=0
    - reward_model.enable=False
    - reward_model.reward_manager=naive
    - custom_reward_function.path=src/reward_wrapper.py
    - custom_reward_function.name=compute_score
    - trainer.logger=[console]
    - trainer.project_name=transluce_test
    - trainer.experiment_name=0.5B_test
    - trainer.n_gpus_per_node=1
    - trainer.nnodes=1
    - trainer.total_epochs=1
    - trainer.val_before_train=False
    - trainer.test_freq=-1
    - critic.model.path=null
    - critic.model.tokenizer_path=null
  job:
    name: main_ppo
    chdir: null
    override_dirname: +ray_init.runtime_env.pip=null,+ray_init.runtime_env.working_dir=/root/transluce-pathological-behaviors,actor_rollout_ref.actor.fsdp_config.optimizer_offload=False,actor_rollout_ref.actor.fsdp_config.param_offload=False,actor_rollout_ref.actor.kl_loss_coef=0.001,actor_rollout_ref.actor.kl_loss_type=low_var_kl,actor_rollout_ref.actor.optim.lr=1e-6,actor_rollout_ref.actor.ppo_micro_batch_size_per_gpu=1,actor_rollout_ref.actor.ppo_mini_batch_size=1,actor_rollout_ref.actor.strategy=fsdp,actor_rollout_ref.actor.use_kl_loss=True,actor_rollout_ref.model.enable_gradient_checkpointing=False,actor_rollout_ref.model.path=Qwen/Qwen2.5-0.5B-Instruct,actor_rollout_ref.model.use_remove_padding=True,actor_rollout_ref.ref.log_prob_micro_batch_size_per_gpu=1,actor_rollout_ref.rollout.gpu_memory_utilization=0.4,actor_rollout_ref.rollout.log_prob_micro_batch_size_per_gpu=1,actor_rollout_ref.rollout.n=4,actor_rollout_ref.rollout.name=vllm,actor_rollout_ref.rollout.tensor_model_parallel_size=1,algorithm.adv_estimator=grpo,algorithm.use_kl_in_reward=False,critic.model.path=null,critic.model.tokenizer_path=null,custom_reward_function.name=compute_score,custom_reward_function.path=src/reward_wrapper.py,data.filter_overlong_prompts=True,data.max_prompt_length=2048,data.max_response_length=1024,data.train_batch_size=1,data.train_files=prepared/train.parquet,data.truncation=error,data.val_files=prepared/train.parquet,reward_model.enable=False,reward_model.reward_manager=naive,trainer.balance_batch=True,trainer.critic_warmup=0,trainer.experiment_name=0.5B_test,trainer.logger=[console],trainer.n_gpus_per_node=1,trainer.nnodes=1,trainer.project_name=transluce_test,trainer.test_freq=-1,trainer.total_epochs=1,trainer.val_before_train=False,~actor_rollout_ref.rollout.engine_kwargs.vllm.disable_mm_preprocessor_cache
    id: ???
    num: ???
    config_name: ppo_trainer
    env_set: {}
    env_copy: []
    config:
      override_dirname:
        kv_sep: '='
        item_sep: ','
        exclude_keys: []
  runtime:
    version: 1.3.2
    version_base: '1.3'
    cwd: /root/transluce-pathological-behaviors
    config_sources:
    - path: hydra.conf
      schema: pkg
      provider: hydra
    - path: /root/transluce-pathological-behaviors/.venv/lib/python3.12/site-packages/verl/trainer/config
      schema: file
      provider: main
    - path: ''
      schema: structured
      provider: schema
    output_dir: /root/transluce-pathological-behaviors/outputs/2026-02-24/18-37-33
    choices:
      hydra/env: default
      hydra/callbacks: null
      hydra/job_logging: default
      hydra/hydra_logging: default
      hydra/hydra_help: default
      hydra/help: default
      hydra/sweeper: basic
      hydra/launcher: basic
      hydra/output: default
  verbose: false
